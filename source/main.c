#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include "table.h"

table *table_hashtags;

int M;

int main(){

	bool le_hashtag = false;

	while (1){
		c = getchar();

		switch (c){

			case 'a':
				while(c = getchar()){

					if(le_hashtag)
						//adiciona ao ponteiro do novo hashtag

					if (c == '#' && !(le_hashtag)){
						le_hashtag = true;

					if(<caracter branco>)
						le_hashtag = false;
					}
				}
				break;

			case 's':
				break;

			case 'm':
				break;

			case 'x':
				break;
		}
	}
	return 0;
}